import { Router } from "express";
const router = Router();

// GET  /participantes?id_viagem=  → listar participantes da viagem
// POST /participantes             → adicionar participante

export default router;

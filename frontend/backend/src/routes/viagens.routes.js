import { Router } from "express";
const router = Router();

// GET    /viagens         → listar viagens do usuário
// GET    /viagens/:id     → detalhes da viagem
// POST   /viagens         → criar viagem
// PATCH  /viagens/:id     → atualizar status

export default router;
